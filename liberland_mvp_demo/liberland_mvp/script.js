const experiences = { student: { title: 'Future Student Experience', body: 'Recommended programmes, admissions guidance, scholarships, open days and a guided application route are prioritised.' }, tutor: { title: 'Tutor Experience', body: 'Teaching opportunities, faculty onboarding, course publishing, office hours and student engagement insights are prioritised.' }, researcher: { title: 'Researcher Experience', body: 'Research centres, publications, innovation labs, funding opportunities and collaboration spaces are prioritised.' }, partner: { title: 'Industry Partner Experience', body: 'Executive education, graduate talent, innovation challenges, internships and research partnerships are prioritised.' } };
function setRole(role, el) { document.querySelectorAll('.role').forEach(b => b.classList.remove('active')); el.classList.add('active'); const e = experiences[role]; document.getElementById('experiencePanel').innerHTML = `<h3>${e.title}</h3><p>${e.body}</p><p><strong>Next best action:</strong> ${role === 'student' ? 'Explore AI & Digital Governance' : 'Open tailored workspace'}</p>` }
function selectCourse(name) { const p = document.getElementById('coursePanel'); p.classList.remove('hidden'); p.innerHTML = `<p class="eyebrow">Selected Journey</p><h3>${name}</h3><p>This learning journey shows outcomes, skills, tutors, community spaces and career pathways instead of a static syllabus.</p><button class="primary" onclick="scrollToSection('admissions')">Start Application</button>`; p.scrollIntoView({ behavior: 'smooth' }); }
function submitApplication(e) { e.preventDefault(); document.getElementById('dashboard').classList.remove('hidden'); alert('Application submitted. Demo student dashboard unlocked.'); scrollToSection('dashboard'); }
function scrollToSection(id) { document.getElementById(id).scrollIntoView({ behavior: 'smooth' }); }
setRole('student', document.querySelector('.role'));
